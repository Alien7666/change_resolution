# Configurable Tray 驗收紀錄

日期：2026-09-18。分支：`codex/configurable-tray`。初始檢查版本：`dc82508`（Task 16 程式提交 `2bbc7c2`）。Task 17 修正提交：`a649920`。以下自動檢查已在修正凍結後重新執行；提交前再次比對，確認受審程式碼與通過驗證的版本相同。

本紀錄區分自動檢查、唯讀實機檢查與仍需人工觀察的顯示變更。未量測的項目不視為通過，也不代表已發布版本。

## 自動檢查

| 檢查 | 結果 |
|---|---|
| `gofmt -l ./internal ./cmd` | 通過，無輸出 |
| `go mod tidy -diff` | 通過；工作目錄的 CRLF 已正規化為 LF，版本與雜湊未改變，沒有依賴檔案差異 |
| `go build ./...` | 通過 |
| `go vet ./...` | 通過 |
| `go test -count=1 ./...` | 通過 |
| `go test -count=20 ./internal/app ./internal/scaling` | 通過 |
| 獨立全分支審查 | 已完成；五項問題均經最後複審接受，Requirements PASS、Code quality PASS WITH MINOR FOLLOW-UPS，沒有阻擋項目 |
| 最終 `build.ps1`、差異與產物檢查 | 通過；產物 `dist/ResolutionTray.exe`（7,247,360 bytes）；沒有將產物納入版控 |
| `go test -race ./...` | 本機未執行；目前 CI 工作流程有設定，這不代表本分支 CI 已通過 |

最終建置第一次遇到 Windows 暫時鎖住 `config.test.exe`，所有測試顯示通過但清理程序退出碼為 1；重跑 `build.ps1` 後退出碼為 0。之後另跑未快取的全套測試及 app/scaling 各 20 次均通過。

## 審查後修正範圍

1. 同型號螢幕的唯一性依型號鍵計算，不能被完整硬體 ID 的不同實例尾碼誤導。
2. 歷史 Mi Monitor 的首次設定預填使用相同型號鍵，並拒絕有歧義的情況。
3. 更換／重新載入設定不得遺失尚未完成的縮放還原義務；拒絕時不得先寫入新設定。
4. 顯示套用已可能改變桌面、但讀回或回滾失敗時，必須保留原始排列與身分綁定供恢復。
5. 執行檔 manifest 改為可設定顯示工具的產品描述。

上述修正已實作、通過聚焦測試與 root 完整自動檢查，最後獨立複審也已通過，提交為 `a649920`。更換設定前的檢查涵蓋顯示管理、待恢復排列與縮放管理；儲存新檔與舊工作階段失效會共同排除延遲的舊設定回寫。

複審留下兩項不阻擋本次收尾的維護事項：移除已無正式呼叫端的 `settingsModel.Save` 及舊流程註解；補上 Provider 在閒置工作階段中儲存失敗的直接回歸測試。審查已確認正式介面走 `SaveAndReplace`，且儲存失敗會保留舊工作階段；這兩項不是已發現的執行期缺陷，保留作後續維護。

## 唯讀實機檢查

`RUN_DISPLAY_INTEGRATION=1` 的顯示器名稱／模式列舉及 Mi Monitor 的 `CDS_TEST` 通過。`RUN_NVAPI_INTEGRATION=1` 的顯示設定讀取、錯誤結構版本負向測試與正式 controller 讀取均通過。沒有使用顯示套用或 NVAPI 寫入。

| 顯示器 | 當時裝置名稱 | 模式（32 bpp） | 桌面座標 | NVAPI 縮放 Raw |
|---|---|---|---|---|
| Mi Monitor | `\\.\DISPLAY1` | 2560 × 1440 @ 180 Hz | (0, 0) | 2，全螢幕／GPU |
| XV272K V3 | `\\.\DISPLAY2` | 3840 × 2160 @ 160 Hz | (2560, 0) | 6，長寬比／顯示器 |
| ZOWIE XL LCD | `\\.\DISPLAY3` | 1920 × 1080 @ 120 Hz（NVAPI：119.982 Hz） | (642, -1080) | 6 |
| 2461W | `\\.\DISPLAY5` | 1920 × 1080 @ 60 Hz | (2562, -1080) | 6 |

這些裝置名稱只是本次觀察，不可作為後續寫入的固定識別。

## 已完成的介面檢查

2026-09-17 使用正式建置產物與獨立的 `RESOLUTION_TRAY_CONFIG` 路徑：

- 首次設定切換 Mi Monitor 與 XV272K V3，縮放提醒隨選定螢幕的讀值更新。
- 在第二台螢幕選取非原生模式並儲存測試設定；沒有套用顯示或縮放設定。
- 主視窗、非原生提醒、手動覆寫提醒與長設定檔錯誤均完整可見。
- 原生 16:9、遊戲模式 4:3、明確指定恢復模式 4:3 時，主視窗仍正確提示非原生比例。
- 測試程序已結束；使用者的實際設定檔未被替換。

2026-09-18 使用 Task 17 新建置產物補驗：

- 首次設定已自動選取 Mi Monitor、1920 × 1440 @ 180 Hz 與 `VALORANT-Win64-Shipping.exe`，並顯示既有設定的確認說明。
- 單次點選儲存後，隔離測試檔包含真實完整介面路徑／硬體 ID、正確模式、原始 fallback 與 3 秒延遲；主視窗仍顯示目前原生 2560 × 1440 @ 180 Hz，未套用遊戲模式。
- 使用另一個不存在的設定路徑啟動，改選 XV272K V3 後按「稍後再設定」，確認設定檔與 `.tmp` 均不存在。
- 本次兩個測試程序（PID 3872、62320）已結束。再次唯讀查詢確認四台模式、刷新率、色彩深度、位置、`DISPLAYn` 與縮放值都與上表一致。

## 人工發布驗收

以下狀態對應主計畫末尾的七個情境。Fake 測試通過不能取代這些實機觀察。

| 情境 | 目前狀態 |
|---|---|
| 非原生模式縮放寫入、讀回、還原，以及管理中的完整循環 | 待受監督實測 |
| 從較小模式放大、鄰居位置與還原 | 待受監督實測；混合一軸放大一軸縮小的中間排列仍有已知限制 |
| 兩台相同型號、換接埠後拒絕錯誤對應 | 當前四台型號不同，未實測此硬體組合 |
| 第二台螢幕首次設定、稍後再設定、儲存後套用 | 選擇、儲存及稍後設定不留檔已通過；實際模式套用待實測 |
| 原始 Mi Monitor 設定的自動預填與相同行為 | 自動預填、說明與單次儲存已在新產物通過；模式切換／觀察程式恢復行為待實測 |
| AMD／Intel 縮放拒絕提示與獨立模式切換 | 當前可用顯示路徑皆由 NVAPI 讀取；未實測非 NVIDIA 機器 |
| 啟動唯讀、手動恢復、自動恢復、延遲中手動取消、系統匣 | 啟動唯讀已檢查；涉及模式寫入的部分待實測，系統匣互動待補齊 |

### 建議的受監督操作順序

兩種情境必須分開量測：下列受管理循環會先恢復原始模式，才寫入縮放，因此不能證明「實際桌面仍在非原生模式時寫入縮放」的結果。後者需由使用者先在工具外切換到非原生模式，再讓尚未取得顯示管理權的工具執行縮放寫入及還原，逐步記錄讀回值、每台模式／位置與裝置名稱，最後將桌面恢復到基線。兩種情境均待使用者在場觀察；目前尚未執行。

完成程式審查後，使用另一份測試設定檔選取 **XV272K V3** 與已列舉出的 **2048 × 1536 @ 160 Hz**。其目前縮放為 Raw 6，比已是 Raw 2 的 Mi Monitor 更適合驗證是否真的發生縮放寫入。所有步驟使用應用程式重新解析顯示器，不依賴上表的 `DISPLAYn`。

1. 再讀一次四台螢幕的模式、位置、主要顯示器、裝置名稱與縮放值，記錄當下基線。
2. 手動套用測試模式；觀察四台螢幕與滑鼠跨螢幕邊界，記錄模式與排列。
3. 按縮放按鈕，觀察恢復原始排列 → 變更縮放 → 重新套用測試模式的三段循環；記錄要求值與讀回值，不能把不一致記成成功匹配。
4. 使用縮放還原按鈕，再觀察完整循環並記錄讀回結果；不假設驅動忠實還原 Raw 6。
5. 恢復顯示排列並退出，核對每台螢幕的模式、座標與縮放結果。若恢復失敗，保留應用程式與錯誤資訊，不進行後續變更。

每次縮放寫入都可能重新編號；應逐步記錄，而不是只比較起點與終點。上述步驟沒有涵蓋所有七個情境，也不能用來宣稱未具備的同型號／非 NVIDIA 硬體情境已通過。
